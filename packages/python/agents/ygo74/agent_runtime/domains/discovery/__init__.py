"""Agent capability discovery domain.

Holds the single source of truth for agent identity and capabilities
(:class:`AgentDescriptor`), the registry that owns uniqueness and ordering, and
the stateless projections that render descriptors into provider-compatible and
A2A wire shapes.
"""
